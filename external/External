# -*- mode: cmake -*-

cmake_minimum_required(VERSION 2.8)

add_custom_target(External)

include(external/MPI)
include(external/Lapack)
include(external/Eigen)
if (TILEDARRAY_NEEDS_BOOST)
  include(external/Boost)
endif()
include(external/MADNESS)


